# AgentHub 환경 변수 설정
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요.

# ============================================================
# LLM API Keys (사용하는 서비스만 설정)
# ============================================================

# Anthropic Claude
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenAI GPT
OPENAI_API_KEY=your-openai-api-key

# Google Gemini
GOOGLE_API_KEY=your-google-api-key

# ============================================================
# Phase 2: Nested Settings Configuration
# ============================================================
# 설정 우선순위: 환경변수 > .env > configs/default.yaml > 기본값
# 중첩 구조: SERVER__HOST 형태로 환경변수 설정
# ============================================================

# Server Settings (선택적, defaults in configs/default.yaml)
# SERVER__HOST=localhost
# SERVER__PORT=8000

# LLM Settings (선택적)
# LLM__DEFAULT_MODEL=anthropic/claude-sonnet-4-20250514
# LLM__TIMEOUT=120

# Storage Settings (선택적)
# STORAGE__DATA_DIR=./data
# STORAGE__DATABASE=agenthub.db

# Health Check Settings (선택적)
# HEALTH_CHECK__INTERVAL_SECONDS=30
# HEALTH_CHECK__TIMEOUT_SECONDS=5

# MCP Settings (선택적)
# MCP__MAX_ACTIVE_TOOLS=30
# MCP__CACHE_TTL_SECONDS=300
